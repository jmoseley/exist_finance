import * as React from 'react';

import LoggedComponent from '../lib/logged_component';

class Callback extends LoggedComponent {
  public render() {
    return <div>Loading...</div>;
  }
}

export default Callback;
